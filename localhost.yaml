all:
  children:
    macos:
      hosts:
        target:
          ansible_connection: local
          anible_host: localhost
          ansible_python_interpreter: "{{ ansible_playbook_python }}"
    ubuntu:
      hosts:
        target:
          ansible_connection: local
          anible_host: localhost
          ansible_python_interpreter: "{{ ansible_playbook_python }}"
    archlinux:
      hosts:
        target:
          ansible_connection: local
          anible_host: localhost
          ansible_python_interpreter: "{{ ansible_playbook_python }}"
